nodes:
  - id: mcp-server
    build: cargo build -p dora-mcp-server --release
    path: ../../target/release/dora-mcp-server
    outputs:
      - counter
      - weather
    inputs:
      counter_reply: counter/reply
      # weather_reply: weather/reply
    env:
      MCP_SERVER_CONFIG: config.toml

  - id: counter
    path: nodes/counter.py
    inputs:
      text: mcp-server/counter
    outputs:
      - reply

  # - id: weather
  #   path: nodes/weather.py
  #   inputs:
  #     text: dora-mcp-server/weather
  #   outputs:
  #     - reply
